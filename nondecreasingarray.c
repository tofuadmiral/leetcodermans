bool checkPossibility(int* nums, int numsSize) {
    
    // if it's non decreasing by modifying an element
    // then tell the user that it's true

    int i = 0;
    // iterate through array 
    while(i<numsSize){
        
    }

    return true;
    
}